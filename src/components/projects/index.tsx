import React from "react";
import PropTypes from "prop-types";
import SectionHeader from "components/common/SectionHeader";
import { Box } from "@mui/material";

const Projects = () => {
  return (
    <Box width="100vw" sx={{ border: "3px solid" }}>
      <SectionHeader />
    </Box>
  );
};

Projects.propTypes = {};

export default Projects;
